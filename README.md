# MSX Models DB

A single static web page presenting MSX2, MSX2+, and MSX turbo R computer model specifications. The purpose is to easily get the overview of the myriad of models ever released. Compare, review and understand the similarities. Find outliers and hallmarks. We see that we have opened a pandoras box - the number of ways to improve this thing we've started, is wast. Hosted at [bengalack.github.io/msx_models_db](https://bengalack.github.io/msx_models_db/)

Features:
* Sortable, filterable, selectable spreadsheet-like grid. 
* All states are encoded in the "future-proof URL" hash so any view can be bookmarked or shared.
* "Future-proof URL": Additions of data, including new columns are expected, and will still work.
* Works on `file://`, GitHub Pages, as a Blogger/Blogspot embed, as well as normal webservers.

The second and most important part of this project is the *scraper*, written in python, which builds the `data.js` dataset used by the web page.

Main data sources are:
1. [openmsx](https://openmsx.org/) and its supported models ([xml files](https://github.com/openMSX/openMSX/tree/master/share/machines)) (publicly accessible)
2. the [ROM files](systemroms/machines/) which are referenced in the xml files (copyrighted, obtain separately)
3. [msx-org's wiki](https://www.msx.org/wiki/Main_Page) pages (publicly accessible)

In addition there are some local files which is used to compliment the data above.

### Flow
The scraper gets its data from the web and produces these files:
* `openmsx-raw.json`
* `msxorg-raw.json`

When this is done, a merge process takes the results from the two above + a local `local-raw.json` and generates the final dataset using specific format and conflict rules.

By design, this scraper should be re-run from time to time to automatically pick up additions and modifications in its datasources.

### Local/Mirror file support
Mid-development of this project, msx.org experience huge traffic from general global AI, which results in slowdowns of their site. Their admins were forced to start denying scraping of their wiki. To remedy this, the scraper can use a local mirror instead (see `data/scraper-config.json`). Such a copy is done with "Save as" in your browser. This does not scale well, but is a needed fallback. Developing this tool using local files turned out to be a must anyway, due to development speed. Hopefully the access can be opened up again, as updates on their wiki is not landing in your mirror that easily.

### Data completeness
The amount of models are massive. Not all are covered, as the available information is not consistently structured or even available. For example, there are sections on msx.org that we currently do not parse, like [Yamaha_CX5M](https://www.msx.org/wiki/Yamaha_CX5M) having a table listing up 7 regional variants of the model having different VDPs and more.


## Background story
This project came to be for two reasons. The author had the need for:
1. A tool like this
2. A real life, sane and usable project to test out developing fully using AI and AI agents.

It has been developed using Claude (Sonnet and Opus 4.6). This project has been using the skills from Leif Eric's [Claude Code Agentic Toolkit](https://github.com/leifericf/claude-code-toolkit)  (included as part of this project) as well as [superpowers](https://github.com/obra/superpowers).

## Prerequisites

- [Node.js](https://nodejs.org/) 20 or later
- [Python](https://www.python.org/) 3.11 or later

## Setup

```sh
# Install JavaScript dependencies
npm install

# Install Python dependencies (use a virtual environment)
python -m venv venv
source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

## Commands

| Command | Description |
|---|---|
| `npm run dev` | Start Vite dev server at `http://localhost:5173` |
| `npm run build` | Build production output to `docs/` |
| `npm run typecheck` | Run TypeScript type checks (no emit) |
| `npm run lint` | Run ESLint on `src/` |
| `npm test -- --run` | Run Vitest test suite once |
| `python -m scraper` | Run the MSX data scraper |

## GitHub Pages

The `docs/` directory is committed to the repository and served directly by GitHub Pages.

To enable GitHub Pages:

1. Go to **Settings → Pages** in the repository.
2. Set **Source** to `Deploy from a branch`.
3. Set **Branch** to `main` and **Folder** to `/docs`.
4. Save. The page will be available at `https://<username>.github.io/<repo>/`.

> The `docs/data.js` file (generated by the scraper) must be hosted on GitHub Pages for the Blogger/Blogspot embed to load data correctly. The CORS headers served by GitHub Pages allow cross-origin script loads.

## Other credits

This project used FontAwesome Free ([CC BY 4.0](https://creativecommons.org/licenses/by/4.0/))

Author also uses [rtk](https://github.com/rtk-ai/rtk) which may have some related commits.
